# research (plugin)

Thinking-vault plugin: the EMBED → QUERY → RENDER pipeline (research-vault, analyze-vault,
vault-artifact). **Scaffold** — real content is migrated from `~/Claude/Code/research` in
**TASK-1.5**, after the gates are ported to Node in **TASK-1.3**.

Role in the suite: **ground** (gather cited facts). Placement: **drop-anywhere** — a vault can
live in any folder, detected via a sentinel marker.
