# educate (plugin)

Socratic learning-project plugin. **Scaffold** — real content is migrated from
`~/Claude/Code/educate` onto the shared `lib/` chassis in **TASK-1.4**; the build/implement
leg is split out into the `build` plugin in **TASK-1.8**.

Role in the suite: **teach** + author the build SPEC. Placement: a favored home folder
(detected via a `topics/` marker).
