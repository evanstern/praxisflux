{
  "configVersion": 1,
  "_comment": "Model IDs here are in THIS HOST's accepted form, which is not the bare API ID. This checkout routes through 9router (see ANTHROPIC_BASE_URL), which augments model names and requires the cc/ prefix and the [1m] context suffix; the bare `claude-sonnet-5` and the `sonnet` alias are both rejected in agent-def frontmatter here (verified 2026-08-10). Resolve your own host's accepted form before editing — see the bootstrap skill's Model tiers section.",
  "defaultTier": "sonnet",
  "escalationTier": "opus",
  "tiers": {
    "haiku": {
      "model": "cc/claude-haiku-4-5-20251001",
      "contextWindow": "200K",
      "for": "narrow mechanical slices — single-file edits, corpus hygiene, tests written to a sibling standard",
      "caution": "200K context (the smallest tier): sized for one phase-scoped dispatch, not for wide-context reads across a corpus. Escalate rather than truncate."
    },
    "sonnet": {
      "model": "cc/claude-sonnet-5[1m]",
      "contextWindow": "1M",
      "for": "the default implementer — work to an existing pattern or a written spec, where the judgment calls are already made"
    },
    "opus": {
      "model": "cc/claude-opus-5[1m]",
      "fallback": "cc/claude-opus-4-8[1m]",
      "contextWindow": "1M",
      "escalation": true,
      "for": "escalation only — design work, cross-surface doctrine, anything with a real judgment call the spec does not already settle"
    }
  }
}
