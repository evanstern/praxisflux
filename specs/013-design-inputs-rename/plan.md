# 013-design-inputs-rename — plan

1. Inventory: git mv the dir; grep -rn "docs/handoffs" (excluding backlog/) for live
   references; check which wiki notes list moved files in sources:.
2. R2: update references; fix sources: lists; re-verify + re-pin affected notes.
3. R3: docs/design-inputs/README.md (the distinction) + one reciprocal sentence in
   docs/handoff-protocol.md.
4. R4: marketplace 0.21.0 (team-review README is released surface); gates green.
5. Orchestrator: finalize; PR; merge; sweep close.
