# 011-build-listing-decision — plan

1. Read build/README.md, the repo README build row, build/.claude-plugin/plugin.json,
   the marketplace entry, and build/skills/ (what TASK-29 actually shipped).
2. R2: rewrite build/README.md to the real story (skill-only plugin, implement leg);
   fix the repo README row; fix the manifest description if stale + regen catalog.
3. R3: versions (marketplace 0.19.0; skill only if edited); gates; wiki re-pins.
4. Orchestrator: R1 decision record at finalization; PR; merge; then TASK-40 starts.
