# 051 — root-guard hook mis-blocks board commits whose message contains a newline or ')'

**Claim stub.** Reserves spec number 051 for TASK-101 and arms the spec-bridge gate from
this branch's first commit. Not the spec — `spec.md`, `plan.md`, and `tasks.md` are
authored in full on this branch before any implementation dispatch, per
`docs/design/gates-and-doctrine-sweep-runbook.md` (operator ruling B: SHIP the hook).

Board task: TASK-101.
