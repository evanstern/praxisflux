# 048 — pdlc:bootstrap plants a model-tier rubric (TASK-91)

**Status: CLAIM STUB.** This file exists to claim spec number 048 in the branch's first
commit, per the sweep's claim-before-work rule. It is not the spec — the real `spec.md`,
`plan.md`, and `tasks.md` are authored on this branch before any implementation dispatch,
and the runbook's "Per-task artifacts required before PR" gate refuses a PR until they are
real.

- Board card: TASK-91 — *pdlc:bootstrap plants no model-tier rubric, but pdlc:sweep hard-requires one*
- Runbook: `docs/design/bootstrap-tier-rubric-runbook.md` (signed-off 2026-08-01)
- Branch: `task-91-bootstrap-tier-rubric`
- Worktree: `.worktrees/task-91`
