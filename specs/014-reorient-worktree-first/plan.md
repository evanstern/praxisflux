# 014-reorient-worktree-first — plan

1. Read reorient/scripts/run.mjs (begin, list, describeOwner — TASK-52's shapes),
   reorient gate tests, SKILL.md.
2. R1 detection + refusal + --shared-checkout; R2 manifest field + list/provenance
   surfacing; tests.
3. R3 SKILL.md doctrine; R4 versions + wiki re-pin (watch reorient-plugin's body cap).
4. Orchestrator: finalize, PR, serial merge.
