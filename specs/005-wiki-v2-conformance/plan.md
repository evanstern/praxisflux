# 005-wiki-v2-conformance — plan

1. Measure: script the char counts of every note's description and body; list violations.
2. R1: rewrite over-budget capsules for routing (honest, not truncated).
3. R2: split build-and-release.md summary-style (1–2 children, real sources subsets,
   verified pins); INDEX.md additive lines; re-measure the corpus.
4. R3: generate docs/wiki/CAPSULES.md via grounding-wiki/scripts/capsules.mjs as the
   last content step; regenerate if any later edit touches a description.
5. R4: prove — wiki-freshness green with zero budget warnings; tests; check-docs;
   per-task course docs/courses/TASK-50.
6. Board finalized; PR; merge; re-ground; sweep close (orchestrator).
