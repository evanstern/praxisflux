# 008-principles-p2-refinements — plan

1. Read docs/principles.md (P1/P2 voice + reference-and-apply contract), TASK-32's
   implementation notes on the board, pdlc/templates/CLAUDE.md stamped region + the
   template-carries-principles test.
2. R1/R2: amend P2 — reason-to-approve test + three-tier model, task-system-agnostic.
3. R3: sync the template's stamped restatement if it quotes P2 at the refined level;
   bump versions if the template changes.
4. R4: wiki re-pins per the freshness gate; CAPSULES.md regen if needed.
5. Course per policy at merge time (orchestrator's call). Gates green; board finalize;
   PR; serial merge.
