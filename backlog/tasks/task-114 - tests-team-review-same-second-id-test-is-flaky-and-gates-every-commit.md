---
id: TASK-114
title: 'tests: team-review same-second id test is flaky and gates every commit'
status: In Progress
assignee:
  - '@claude'
created_date: '2026-08-28 18:30'
updated_date: '2026-09-01 15:23'
labels:
  - tests
  - debt
  - flake
dependencies: []
ordinal: 146000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`test/team-review.test.mjs:206` — "a same-second begin never overwrites — ids stay distinct" — fails intermittently in full-suite runs.

Observed 2026-08-28 during TASK-104 (spec 058): a dispatched implementer hit it on two consecutive full-suite runs (442/443), while the same file passed 16/16 in isolation and the pre-commit hook's own full-suite run passed clean. A follow-up probe ran the file 12x in isolation: 12 pass / 0 fail. Two independent full-suite runs afterwards: 443/443 green both times.

So it is rare, real, and load-bearing in the wrong way: the suite is the gate that blocks every commit on this repo (`core.hooksPath` + `.githooks/pre-commit` run the full suite), and it is also what the spec-bridge project-gate check consults before allowing a ticked tasks.md box. A test that fails a few percent of the time therefore randomly blocks commits and randomly reports honest board state as dishonest.

The test appears to be a probabilistic collision-retry: it asserts distinct ids across attempts within the same second. Likely fix is to make id generation deterministic under test (inject a counter/clock) rather than retrying and hoping — but the actual mechanism should be read before choosing.

Unrelated to spec 058, which touches only lib/spec-source.mjs and lib/spec-derive.mjs. Carded rather than fixed in-scope, per the sweep's scope-discipline rule.

Spec: specs/059-flake-deterministic-clock
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The intermittent failure at test/team-review.test.mjs:206 is reproduced or its mechanism explained from the code
- [ ] #2 Id generation under test is made deterministic (or the assertion made non-probabilistic) so the test cannot fail by chance
- [ ] #3 The full suite passes across at least 20 consecutive runs
- [ ] #4 Spec phase: Phase 1 — The seam and the deterministic test
- [ ] #5 Spec phase: Phase 2 — Prove it, bump, re-ground
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
MECHANISM CONFIRMED from the code (2026-09-01, orchestrator; AC#1 evidence). test/team-review.test.mjs:206-223 computes stamp = new Date() truncated to the second, pre-writes a record for that stamp, then SPAWNS the CLI as a subprocess and asserts the id came back with the collision suffix ('x'). The assertion at :222 — assert.ok(collided, 'collision suffix path never exercised across 3 attempts') — holds only if the subprocess computes the SAME second the test did. If the wall clock crosses a second boundary during spawn latency, no collision occurs and that attempt is wasted. It retries 3x, so failure needs all three attempts to straddle a boundary — rare when the machine is idle, much likelier under a loaded full-suite run where spawn latency is both larger and more variable. That matches every observation: 12/12 and 16/16 pass in ISOLATION (fast spawns, boundary rarely crossed), intermittent failure only in FULL-SUITE runs. Measured suite duration on this host: 46.2s / 48.2s / ~45s, i.e. sustained load throughout. So AC#2's 'make it deterministic rather than retrying and hoping' is the right fix and the retry loop is the thing to remove: inject the clock (or the stamp) into the CLI under test so the test controls the second, instead of racing a real one. NOTE the blast radius is wider than 'a flaky test': the full suite is the 'tests' project gate the spec-bridge Stop hook consults, and a Done-eligible board amplifies ONE red gate into ~50 findings (one per Done-eligible spec) — observed 2026-08-28 (TASK-102) and again 2026-09-01 during TASK-109, where 55 findings all read 'the required gate "tests" is red (exited 1)'. Ruled out as causes on 2026-09-01, so a future session need not re-derive them: (1) NOT a gate-timeout — GATE_TIMEOUT_MS is 120000 and a single suite run is ~46s, and a timeout would have printed 'timed out after 120000ms', not 'exited 1'; (2) NOT the SPEC_BRIDGE_GATE_ACTIVE reentrancy guard — the suite exits 0 with 468/468 with that env var set; (3) NOT gate misattribution in the message builder — projectGateProblem/gateReason name whichever gate actually failed, and the two runs simply saw two different real failures (the flake first, then the expected mid-PR wiki-freshness staleness).

CLAIMED by sweep orchestrator 2026-09-01. Folded into Lane 2 at operator request as a THIRD parallel item with its own branch and PR — TASK-114 is a top-level TASK, so one-TASK-one-PR means it does not ride TASK-110's or TASK-111's branch. Branch task-114-flake-fix off origin/main tip 5a9fce0; worktree .claude/worktrees/task-114 (background-job mode). Unlike 110/111, this task had NO pre-existing spec, so the orchestrator hand-authored specs/059-flake-deterministic-clock (spec.md + plan.md + tasks.md) under the runbook's operator-signed escape line — proportionate to a ~3-line fix, with the confirmed mechanism recorded in tasks.md Notes so the implementer does not re-derive it. TIER: sonnet · cc/claude-sonnet-5[1m] (defaultTier). NOTE: an orchestrator slip clobbered this card's description during the claim (a bad shell substitution passed empty text to -d); recovered verbatim from git HEAD and restored with the Spec marker appended. No content lost; the earlier mechanism note survived.

ORCHESTRATION CONSTRAINT FOUND (2026-09-01, orchestrator error — recording so no future sweep repeats it). Dispatched subagents inherit the ORCHESTRATOR SESSION'S current worktree for their Bash sandbox, not the worktree named in their dispatch prompt. Consequence: an orchestrator cannot host concurrent implementer dispatches across sibling worktrees by switching its own session between them — switching the session breaks the Bash tool of every already-running agent, which then correctly refuses to act (guard message: 'this command's working directory resolved to the shared checkout <other worktree>'). Observed concretely: TASK-110 Phase 1 and TASK-111 Phase 1 were dispatched, then the session moved on to claim TASK-111 and TASK-114; BOTH agents lost Bash and stopped, having made zero edits and zero commits. Both behaved correctly — they refused to operate in a sibling worktree they had no authority over, and refused to edit code they could not test or commit. No cleanup needed; both worktrees clean. REMEDY for the rest of this lane: dispatch SERIALLY, with the orchestrator session parked in the target worktree for the whole duration of that dispatch. 'Develop in parallel' in the runbook means parallel BRANCHES (which is real and holds — claims are pushed and independent), not concurrent dispatches from one orchestrator session on this harness. The runbook's Lane 2 line should be amended to say so.

PHASE 1 COMPLETE (commit 2fa9511), verified independently by the orchestrator. Suite 468 -> 469/469, 0 fail. Verified in the code, not taken on report: (a) the seam at team-review/scripts/run.mjs:53 is 'process.env.TEAM_REVIEW_RUN_STAMP || new Date()...slice(0,19)' — default is today's expression VERBATIM, so production ids are byte-identical (AC#3), and it is commented as a test seam with an explicit 'never document it' (matching runGateCommand's injected-spawn precedent at bridge.mjs:169 rather than inventing a convention); (b) the rewritten test asserts 'assert.equal(id, `${expected}x`)' by EXACT EQUALITY, not a boolean flag — so the collision-suffix branch 'while (existsSync(runPath(id))) id += "x"' is reached by construction and the test fails outright if it is ever skipped (AC#2, the regression-disguised-as-a-fix risk the plan named); (c) the three-attempt retry loop is GONE; (d) protected files byte-identical (git diff --stat 5a9fce0..HEAD empty over all three, AC#5). Production diff is 8 lines in one file. Phase 1 deliberately did NOT attempt the load-dependent reproduction — correct, that is Phase 2's 20-run proof (AC#4).

20-RUN PROOF COMPLETE AND VERIFIED (2026-09-01) — this is AC#3's artifact. Ran bare 'node --test' 20 consecutive times detached (PID 73949, log $CLAUDE_JOB_DIR/tmp/task114-20run.log). RESULT: 20/20 runs exited 0; every run reported 'pass 469 / fail 0'; grep for 'not ok' and the failure marker across the entire 20-run log returns ZERO; the previously-flaky test 'a same-second begin never overwrites' is present in all 20 runs (so it ran every time and was not skipped). Verified by the ORCHESTRATOR reading the log directly — not accepted from the implementer's report, which had returned early while the proof was still running with nothing watching it (the orchestrator armed a monitor on non-zero exits / run 20 / any fail line and confirmed from the raw log afterwards). Contrast the pre-fix baseline recorded above: intermittent failure in full-suite runs, 442/443 twice in a row during TASK-104. The fix holds.

PHASE 2 COMPLETE — four gates verified INDEPENDENTLY by the orchestrator: node --test 469/469 0 fail; check-docs exit 0; sync-version --check 'all versions = 0.59.1'; wiki freshness exit 0 with 40 notes fresh (only the two pre-existing size-budget warns, non-blocking). Commits: bfac87a (proof recorded), ddd611d (0.59.0 -> 0.59.1 + team-review skill 1.3.0 -> 1.3.1), b3fe130 (12-note re-ground), 8625e44 (Phase 2 ticks + ledger), 8db7430 (cascade re-pin). Version increment is PATCH and that is right per docs/releasing.md: an internal test-determinism fix with production behaviour byte-identical, so nothing anyone depends on changed. VERIFIED, not taken on report: (a) ZERO added or widened size_budget_exempt lines across docs/wiki/ (git diff over the whole branch) — the constraint held, neither over-budget note was touched to absorb new prose; (b) the three protected test files are byte-identical across the whole branch (AC#5); (c) the one genuine prose amendment is honest — docs/wiki/team-review-plugin.md quoted 'version: 1.3.0' literally, the diff shows that corrected to 1.3.1 BEFORE the pin moved, which is the honest-re-pin rule applied correctly rather than a mechanical pin bump. GENERAL FINDING WORTH KEEPING (the implementer caught this, and it is not obvious): re-pinning a note that is ITSELF listed in another note's sources: cascades — bumping test-suite-catalog-plugins-gates.md's verified_against line made its PARENT test-suite-catalog-plugins.md go stale, because the parent sources the child FILE. So a re-ground pass must re-run the freshness gate AFTER committing re-pins, not just before; a single pass can leave the gate red. Caught here by re-running the gate and fixed in 8db7430.

PR #133 OPENED (https://github.com/evanstern/praxisflux/pull/133), branch task-114-flake-fix, 8 commits 5a9fce0..c7c589c. origin/main had not moved, so no reconcile and no pin reclassification was needed. MUST MERGE AS A MERGE COMMIT, never squash — the branch carries wiki pins referencing its own commits (b3fe130 and 8db7430 re-pin 13 notes to ddd611d), and squashing would orphan those hashes and break the freshness gate. Awaiting operator review/merge per the runbook's serial-merge rule; NOT self-merged. Note for the merge order: this PR bumps to 0.59.1, so whichever of TASK-110/TASK-111 merges after it will conflict on .claude-plugin/marketplace.json and every plugin.json and must reconcile with fresh origin/main before opening or updating its PR — that is the expected Lane 2 serial-merge tax, not a surprise.
<!-- SECTION:NOTES:END -->
