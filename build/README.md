# build (plugin)

Implementation plugin. **Scaffold — not yet implemented.** Created by splitting the build leg
out of educate in **TASK-1.8**: it consumes a SPEC (forward handoff), implements + verifies, and
emits findings (return handoff) for the lesson to fold back in.

Role in the suite: **implement**. Rides the shared handoff transport (**TASK-1.7**).
